{% extends 'base.html.twig' %}

{% block body %}
	<main class="container">
		<section class="row">
			<div class="col-12">
				<h1 class="h1">Liste des produits de la catégorie
					{{category.name}}</h1>
			</div>
			{% for product in products.data %}
				{# <div class="col-4">
				
									<article class="card" width="600">
										<h2 class="card-title">{{product.name}}</h2>
										<img src="{{ asset('assets/uploads/' ~ product.image )}}" width="400" height="300" alt="{{ product.name }}">
										<p class="card-text">{{product.description}}</p>
										<a href="{{path('products_details', {"slug" : product.slug})}}" class="btn btn-primary">Voir plus</a>
									</article>
				
								</div> #}
				<div class="col-10 m-auto col-md-4 mb-3 mt-5 ">

					<article class="carte">

						{# <legend class="mt-3 mb-5 legend-article">{{product.name}}</legend> #}
						{# <h2 class="card-title">{{product.name}}</h2> #}
						<img src="{{ asset('assets/uploads/' ~ product.image )}}" style="width: 90%;" height="250" alt="{{ product.name }}" class="mt-3"><legend class=" mt-2 mb-2 legend-article">{{product.name}}</legend>
						<p class="card-text pt-4 m-auto description-article-list" style="width: 80%;">{{product.description}}</p>
						<p class="mt-2"><strong>{{product.price / 100}} €</strong></p>
						<div class="d-flex justify-content-center m-auto mt-5 mb-5">
							<a href="{{path('products_details', {"slug" : product.slug})}}" class="detail me-1 ">Voir plus</a>
							<a href="{{ path('cart_add', {id: product.id}) }}" class="submit ms-1">Ajouter au panier</a>
						</div>

					</article>

				</div>

			{% endfor %}

			{% set path = 'categories_list' %}
			{% set slug = category.slug %}
			{% set pages = products.pages %}
			{% set currentPage = products.page %}
			{# <div class="container-fluid"> #}
			<div class="page-m">{% include '_partials/_pagination.html.twig' %}</div>

			{# </div> #}

		</section>
	</main>
{% endblock %}
